[**TmasPlus — Referencia de código**](../../../../README.md)

***

[TmasPlus — Referencia de código](../../../../README.md) / [common/store/hooks](../README.md) / useAppDispatch

# Function: useAppDispatch()

> **useAppDispatch**(): `ThunkDispatch`\<\{ `bookings`: `BookingsState`; `auth`: `AuthState`; `wallet`: `WalletState`; `vehicles`: `VehicleState`; `promodata`: \{ `error`: `null`; `loading`: `boolean`; `promos`: `any`; \} \| \{ `promos`: `never`[]; `loading`: `boolean`; `error`: `any`; \}; `complains`: `ComplainState`; `memberships`: `MembershipState`; `settings`: `SettingsState`; \}, `undefined`, `UnknownAction`\> & `Dispatch`\<`UnknownAction`\>

Defined in: [common/store/hooks.ts:4](https://github.com/tmasplusempresa-png/prueba2/blob/4901cd1d9dfde0ca0fccf524b740edfeb7ef3df7/App/common/store/hooks.ts#L4)

## Returns

`ThunkDispatch`\<\{ `bookings`: `BookingsState`; `auth`: `AuthState`; `wallet`: `WalletState`; `vehicles`: `VehicleState`; `promodata`: \{ `error`: `null`; `loading`: `boolean`; `promos`: `any`; \} \| \{ `promos`: `never`[]; `loading`: `boolean`; `error`: `any`; \}; `complains`: `ComplainState`; `memberships`: `MembershipState`; `settings`: `SettingsState`; \}, `undefined`, `UnknownAction`\> & `Dispatch`\<`UnknownAction`\>
